:root {
  --bg: #ffffff;
  --fg: #222222;
  --link: #0a58ca;
  --header-bg: #003366;
  --header-fg: #ffffff;
  --focus: #ffbf47;
  --maxw: 960px;
  --font-size: 16px;
}

* { box-sizing: border-box; }
html { font-size: var(--font-size); }
body {
  margin: 0;
  font-family: Arial, Helvetica, sans-serif;
  line-height: 1.6;
  background: var(--bg);
  color: var(--fg);
}

/* Alto contraste e fonte grande */
body.alto-contraste {
  --bg: #000;
  --fg: #fff;
  --link: #0ff;
  --header-bg: #000;
  --header-fg: #0ff;
  --focus: #ff0;
}
body.fonte-grande { --font-size: 18px; }

/* Acessibilidade visual */
.skip-link {
  position: absolute;
  left: -999px;
  top: 0;
  background: var(--focus);
  color: #000;
  padding: 8px 12px;
}
.skip-link:focus { left: 8px; z-index: 1000; }

a { color: var(--link); }
a:hover, a:focus { text-decoration: underline; }

header, footer {
  background: var(--header-bg);
  color: var(--header-fg);
  padding: 1rem;
}
header h1 { margin: 0 0 .5rem; }

.menu {
  display: flex;
  list-style: none;
  gap: 1rem;
  padding: 0;
  margin: .5rem 0 0;
}
.menu a { color: var(--header-fg); text-decoration: none; }
.menu a:focus-visible { outline: 3px solid var(--focus); outline-offset: 2px; }

.acessibilidade-controles {
  margin-top: .75rem;
  display: flex;
  gap: .5rem;
}
.acessibilidade-controles button {
  border: 2px solid currentColor;
  background: transparent;
  color: var(--header-fg);
  padding: .4rem .6rem;
  border-radius: .5rem;
  cursor: pointer;
}
.acessibilidade-controles button:focus-visible {
  outline: 3px solid var(--focus);
  outline-offset: 2px;
  background: rgba(255,255,255,.1);
}

main {
  max-width: var(--maxw);
  margin: 1rem auto;
  padding: 0 1rem;
}

figure { margin: 1rem 0; }
img { max-width: 100%; height: auto; }

.campo { margin: .6rem 0; }
label { display: inline-block; margin-bottom: .25rem; }
input, textarea, button[type="submit"] {
  width: 100%;
  padding: .6rem .7rem;
  border: 1px solid #bbb;
  border-radius: .4rem;
  font: inherit;
}
button[type="submit"] {
  margin-top: .5rem;
  border: none;
  background: #155724;
  color: #fff;
}
button[type="submit"]:focus-visible { outline: 3px solid var(--focus); outline-offset: 2px; }

/* Foco visível global */
:focus-visible {
  outline: 3px solid var(--focus);
  outline-offset: 2px;
}

/* Apenas para leitores de tela */
.sr-only {
  position: absolute !important;
  width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0, 0, 0, 0); white-space: nowrap; border: 0;
}
